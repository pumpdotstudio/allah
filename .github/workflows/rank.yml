name: rank

# DEPRECATED: Ranking moved to pumpdotstudio/intern (continuous pipelined ranker)
# This workflow is disabled. allah now only handles agent spawning.
# All allah agent keys are configured in intern's GHA secrets.

on:
  workflow_dispatch:

jobs:
  deprecated:
    runs-on: ubuntu-latest
    timeout-minutes: 1
    steps:
      - name: Notice
        run: echo "Ranking moved to pumpdotstudio/intern — run that workflow instead"
